# SNMP-Modbus 桥接服务依赖包
# 
# 安装方法：
# pip install -r requirements.txt

# SNMP 协议支持
pysnmp>=4.4.12
pyasn1>=0.4.8

# Modbus 协议支持
pymodbus>=3.0.0

# 异步 I/O 支持（Python 3.7+ 内置，但某些版本可能需要）
# asyncio  # 通常不需要单独安装

# 可选：更好的串口支持（RTU 模式）
pyserial>=3.5

# 可选：性能监控和调试
# psutil>=5.8.0
# memory-profiler>=0.60.0
